# Ask user for their name and remove whitespace from str (with method strip) and Capitalize user's name (premiere lettre de chaque lettre)
name = input("What's your name? ").strip().title()

# Say hello to user
print (f"hello, {name}")
